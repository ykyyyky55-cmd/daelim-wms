-- ==============================================================================
-- [4단계] 예전 아이디/비밀번호 사용자 테이블 삭제
-- ==============================================================================
-- ⚠️ 실행 전 확인: 3단계(02_lock_down_policies.sql) 적용 후 모든 사용자가 새 로그인으로 정상 사용 중인지.
--    wms_users에는 평문 비밀번호가 들어 있으므로, 새 로그인으로 전환이 끝나면 삭제한다.
--    (필요하면 삭제 전에 이름·부서 목록만 따로 백업: SELECT username, name, dept, role FROM public.wms_users;)
-- ==============================================================================

DROP TABLE IF EXISTS public.wms_users;
