-- ==============================================================================
-- wms_recipes: 구버전 보관함으로 직접 옮긴 시방서 표시 (archived)
-- ==============================================================================
-- 제조시방서 목록은 제품별 최신 리비전만 보여주고 나머지는 '구버전 보관함'에 둔다.
-- archived = TRUE 이면 리비전 번호와 관계없이 보관함에 둔다 (목록에서 선택해 '구버전으로 옮기기').
-- '되돌리기'로 archived를 풀면 그 리비전이 다시 최신 목록 후보가 된다.
-- 여러 번 실행해도 안전하다. (기존 RLS 정책 그대로 적용)
-- ==============================================================================

ALTER TABLE public.wms_recipes ADD COLUMN IF NOT EXISTS archived BOOLEAN NOT NULL DEFAULT FALSE;
